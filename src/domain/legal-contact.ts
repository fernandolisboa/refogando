/**
 * Contatos legais (LGPD) — constantes PURAS, client-safe (SEM `process.env`, sem I/O).
 * São valores PÚBLICOS exibidos nas páginas de Privacidade e Seus Direitos. O dono troca
 * fácil aqui: editar estas linhas é a única fonte da verdade para o crédito/encarregado.
 */
export const ENCARREGADO_EMAIL = 'privacidade@refogando.com' // e-mail do encarregado (DPO) — o dono troca fácil
export const ENCARREGADO_NOME = 'Fernando Lisboa' // nome do encarregado (DPO) — o dono troca fácil
export const CONTROLADOR_NOME = 'Fernando Lisboa' // controlador = pessoa física operando o Refogando — o dono troca fácil
export const APP_PUBLIC_URL = 'https://refogando.com' // URL pública do app — o dono troca fácil
